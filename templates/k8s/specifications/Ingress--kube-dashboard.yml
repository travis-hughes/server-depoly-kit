apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-kube-dashboard
  namespace: ingress-kube-dashboard
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: dashboard
      port:
        number: 443